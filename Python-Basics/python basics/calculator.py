print("calculator program")
first_number = input("first number: ")
second_number = input("second number: ")
print(first_number + second_number)#it will simpliy print values of these variables instead of adding them to do so we need to typecast it to int
print(int(first_number) + int(second_number))

      