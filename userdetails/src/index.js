import React from "react";
import ReactDOM from "react-dom";  // Fix the import statement
import App from "./app";

ReactDOM.render(<App />, document.getElementById("root"));  // Fix the render method
